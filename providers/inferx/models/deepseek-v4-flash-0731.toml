# Toggle wire: {"chat_template_kwargs": {"enable_thinking": true|false}} — verified live on InferX
# Effort wire: {"reasoning_effort": "high"|"max"} — verified live on InferX, both populate reasoning output
base_model = "deepseek/deepseek-v4-flash-0731"

[cost]
input = 0.056
output = 0.112

[[reasoning_options]]
type = "toggle" # API: {"chat_template_kwargs": {"enable_thinking": false}}

[[reasoning_options]]
type = "effort" # API: {"reasoning_effort": "high"|"max"}
values = ["high", "max"]
